---
############################################################
# DISCUSSION:
# 1. Add force option
# - if 'true' then it will delete an existing 'dflt_spot'
# and create a new one from the specified nim_alt_disk_migration_lpp_source
# - if 'false then it will skip the automatic creation
# of a SPOT resource from a nim_alt_disk_migration_lpp_source if 'dflt_spot'
# already exists
# 2. Add feature to specify an existing SPOT resource
############################################################

- name: Fetch the location of the nim_alt_disk_migration_lpp_source
  block:
    - name: Query LPP source resource information
      ibm.power_aix.nim_resource:
        action: show
        name: "{{ nim_alt_disk_migration_lpp_source }}"
      register: nim_alt_disk_migration_lpp_source_query

    - name: Set LPP source info and location pattern
      ansible.builtin.set_fact:
        nim_alt_disk_migration_lpp_source_info: "{{ nim_alt_disk_migration_lpp_source_query.nim_resources.values() | first }}"
        nim_alt_disk_migration_location_pattern: (/\S+)(?=/)

    - name: Extract LPP source location path
      ansible.builtin.set_fact:
        nim_alt_disk_migration_lpp_source_location:
          "{{ nim_alt_disk_migration_lpp_source_info.location | regex_search(nim_alt_disk_migration_location_pattern, '\\0') | join('\n') }}"

############################################################
# NOTE: if SPOT is not specified then create a SPOT
# resource from a specified LPP source
#
# e.g. stderr if SPOT resource of the same name
# already exists
# nim -o define -t spot -a location=/nim1 -a server=master -a source=lpp_2134A_730 dflt_spot
# 0042-001 nim: processing error encountered on "master":
# 0042-081 m_mkspot: a resource already exists on "master" at
#   location "/nim1/dflt_spot/usr"; due to NFS export restrictions, the
#   new location "/nim1/dflt_spot/usr" may not be used
############################################################

- name: Create SPOT resource from {{ nim_alt_disk_migration_lpp_source }}
  ibm.power_aix.nim_resource:
    action: create
    name: dflt_{{ nim_alt_disk_migration_lpp_source }}_spot
    object_type: spot
    attributes:
      source: "{{ nim_alt_disk_migration_lpp_source }}"
      location: "{{ nim_alt_disk_migration_lpp_source_location }}"
  register: nim_alt_disk_migration_result

- name: If SPOT resource already exists then query if 'bos.alt_disk_install.rte is installed'
  when: nim_alt_disk_migration_result.stderr.find( "0042-081" ) != -1

  block:
    - name: Display SPOT resource already exists message
      ansible.builtin.debug:
        msg: SPOT resource 'dflt_{{ nim_alt_disk_migration_lpp_source }}_spot' already exists.

    - name: Query existing SPOT resource for installed filesets
      ibm.power_aix.nim_resource:
        action: show
        name: dflt_{{ nim_alt_disk_migration_lpp_source }}_spot
        showres:
          fetch_contents: true
          max_retries: "{{ nim_alt_disk_migration_dflt_max_retries }}"
          retry_wait_time: "{{ nim_alt_disk_migration_dflt_retry_wait_time }}"
      register: nim_alt_disk_migration_spot_query
- name: Set SPOT resource to be used
  block:
    - name: Set SPOT resource name variable
      ansible.builtin.set_fact:
        nim_alt_disk_migration_spot: dflt_{{ nim_alt_disk_migration_lpp_source }}_spot
    - name: Set SPOT resource fileset contents
      ansible.builtin.set_fact:
        nim_alt_disk_migration_spot_fileset: "{{ nim_alt_disk_migration_spot_query['nim_resources'][nim_alt_disk_migration_spot]['contents'] }}"

- name: Include task to add alt_disk_install fileset if needed
  ansible.builtin.include_tasks: add_alt_disk_install_fileset.yml
  when: >
    nim_alt_disk_migration_result.stderr.find( "0042-081" ) == -1 or
    nim_alt_disk_migration_spot_fileset['bos.alt_disk_install.rte'] is not defined
